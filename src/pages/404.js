import React from "react"
import styled from '@emotion/styled';
import theme from '../../config/theme';
import Layout from '../components/layout';

const Lost = ({data}) => {
    return(
        <Layout>
            <h1>This Page is Melted in the Sun.</h1>
            <p>It is what it is.</p>
        </Layout>
    )
}

export default Lost